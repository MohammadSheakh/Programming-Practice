#include<stdio.h>
int main()
{
    double N, line=1;

    printf("Number of Line : ");

    scanf("%d",&N);

    while(line<=N)
    {
        double arr[10000000], line1=0, A;

        for(i=0; i<=A, i++);
        {
            scanf("%lf", &arr[i]);
        }
        line++;
    }
    return 0;
}
