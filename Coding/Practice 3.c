#include<stdio.h>
#define MAX_SIZE 100
int main()
{
    long double N, line;
    printf("Number of Line : ");
    scanf("%Lf",&N);
    for(line=1;line<=N;line++)
    {
        long double i, MAX_SIZE, arr[MAX_SIZE];
        for(i=0; i<MAX_SIZE; i++)
        {
        scanf("%Lf", &arr[i]);
        }
    }
    return 0;
}
